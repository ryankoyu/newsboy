{
  "name": "briefly-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "BRIEFLY daily content pipeline worker (scaffold only — implementation owned by the pipeline team). See docs/design/a1-architecture.md §2 for the 8-stage flow this will implement.",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "collect": "tsx src/scripts/run-collect.ts",
    "demo": "tsx src/scripts/run-demo.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "@supabase/supabase-js": "^2",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^2.1.9"
  }
}

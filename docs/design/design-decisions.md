# Phase 2 설계 결정 기록 (메인 에이전트 교차 검토)

2026-07-10 — A1(아키텍처)·A2(데이터 모델)·A3(UI/UX) 교차 검토 및 미결 사항 확정.
이 문서가 세 설계 문서 간 충돌 시 **우선 기준**이다.

## 1. 교차 검토에서 발견한 불일치와 조정

| # | 불일치 | 조정 결정 |
|---|---|---|
| 1 | A1은 `fact_provenance` 단일 테이블, A2는 `facts` + `fact_sources` 2테이블(M:N) | **A2가 스키마의 단일 기준.** A1 문서의 `fact_provenance`는 A2의 `facts`+`fact_sources`를 가리키는 것으로 읽는다. |
| 2 | A1이 언급한 `events`(사건 클러스터)·`pipeline_runs`(모니터링) 테이블이 A2 스키마에 없음 | `events`는 불필요 — A2의 `articles`(사건 단위) + `sources`(N:1)가 같은 역할. **`pipeline_runs`는 A2 스키마에 추가 채택** (일일 실행 기록·단계별 상태·오류 로그, 운영 모니터링에 필요). Phase 3 구현 시 반영. |
| 3 | (확인 결과 일치) A3의 화면 요소 ↔ A2 스키마: Saved 탭=bookmarks, 퀴즈=quizzes/quiz_options, 사전 팝업=words(발음 포함) 모두 대응됨 | 조치 불요 |

## 2. A2(데이터 모델) 미결 확정

1. **provenance 공개 범위** → 내부 전용(검수 화면). 사용자에겐 기사 하단 소스 링크만. 감사 페이지 공개는 변호사 자문 후 재론.
2. **단어·퀴즈** → 레벨(version)별 생성 유지.
3. **본문 저장** → 문장 단위 JSONB. (단어 클릭·문장 비교 V1.5·쉐도잉 V2가 전부 문장 단위 인터랙션)

## 3. A1(아키텍처) 미결 확정

1. **파이프라인 실행 환경** → GitHub Actions 스케줄.
2. **백엔드** → Supabase.
3. **재작성 모델** → 상위 모델(Opus급)로 시작, 첫 실측 후 비용 재조정.
4. **provenance 사용자 노출** → MVP는 하단 출처 목록만.

## 4. A3(UI/UX) 미결 확정

1. 강조색 **#C8622D(로스티드 오렌지) 승인** — 단, 가칭 BRIEFLY의 네이밍/브랜딩 확정 시 재검토 대상임을 기록.
2. 영어 본문 **세리프(Lora) 승인**.
3. **비로그인도 Top 10 전부 열람 허용** (R2 발견 — 뉴스 열람 유료화 선례 없음 — 과 일치. 성장 우선).
4. 사전 발음은 **IPA 표기만, TTS 재생 버튼은 V1.5** (MVP 범위와 일치).
5. 읽기시간 wpm 기준 90/120/150 — 초안 승인, 실사용 데이터로 조정.
6. 온보딩 관심사 선택 **제외** (관심사 큐레이션은 V2 — MVP 범위와 일치).
7. 기사 뷰어는 **A2/B1/B2 3칸** (Original 칸 없음. 원문은 하단 출처 링크로 갈음 — 저작권 전략과도 정합).
8. 하단 탭 **Home / Saved / Settings 3탭 확정**.

## 5. Phase 3 이관 사항

- `pipeline_runs` 테이블 추가 (§1-2)
- 운영 준비 체크리스트: a1-architecture.md 부록 참조 (결제 필요 항목은 사용자 확인 필수)
- 첫 파이프라인 실행 후 LLM 비용 실측 보고 → 모델 배분 재조정
